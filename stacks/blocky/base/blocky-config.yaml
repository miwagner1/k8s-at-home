apiVersion: v1
kind: Secret
metadata:
  name: blocky-config
  namespace: blocky
stringData:
  config.yml: |
    upstream:
      default:
        - 1.1.1.1
    blocking:
      blackLists:
        ads:
          - https://raw.githubusercontent.com/StevenBlack/hosts/master/hosts
      clientGroupsBlock:
        default:
          - ads
    port: 53
    httpPort: 4000